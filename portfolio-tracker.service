[Unit]
Description=Portfolio Tracker API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/portfolio-tracker/backend
ExecStart=/opt/portfolio-tracker/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
